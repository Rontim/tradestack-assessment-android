package io.ibuqa.tradestack.collections

import org.junit.Assert.assertTrue
import org.junit.Test

/** Replace this. It exists so `gradle testDebugUnitTest` has something to run. */
class SanityTest {
    @Test
    fun theProjectCompiles() {
        assertTrue(true)
    }
}
